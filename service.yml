apiVersion: v1
kind: Service
metadata:
  name: my-app-service
spec:
  selector:
    app: my-app
  ports:
  - protocol: TCP
    port: 80  # The port the service is exposed on
    targetPort: 80  # The container port to forward to
  type: LoadBalancer
